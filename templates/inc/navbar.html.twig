<nav class="navbar navbar-expand-lg navbar ">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" class="droite" id="navbarNav">

        {% if is_granted('ROLE_USER') %}
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('products') }}" class="nav-link">Produits</a>
                </li>
            </ul>
            <ul class="nav navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}" tabindex="-1" aria-disabled="true">déconnexion</a>
                </li>
            </ul>
        {% elseif not app.user %}
            <ul class="navbar-nav">
                <a href="/" class="navbar-brand mb-0 h1">Commerce</a>
            </ul>
            <ul class="nav navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                </li>
            </ul>
        {% endif %}
    </div>
</nav>
