<div class="card">
    <div class="card-body">
        <h5 class="card-title">{{ product.product.product.name }}</h5>
            <div class="row">
                <div class="ecartGauche">
                </div>
                <div class="" >
                    Moyenne :
                      {% include 'inc/rating.html.twig' with {'rating': product.avgRating } %}
                </div>
                <div  style="margin-bottom:20px;">
                    <small>
                        moyenne calculée sur {{ product[1] - 1 }} avis
                    </small>
                </div>
                <div class="ecartDroite">
                    <a href="{{ path('infoproduct',{'id':product.product.product.id}) }}" class="btn btn-success float-right">
                        Votez pour moi
                    </a>
                </div>
            </div>
    </div>
</div>